# Development

Architecture, dual-mode, epics/phases, kickoff workflow — **product-owned** docs.

Scaffolded by `create-saas` as an empty tree + this README. Add:

- `architecture-overview.md` — whole-picture system map
- `dual-mode-overview.md` — saas vs selfhosted
- Epic / phase folders as you plan work

Kit standards (tenancy, AppShell) live in `@llanesleonardo/saas-product-shell` docs; **do not** re-document the kit here unless product-specific.

**Agents:** skill **saas-docs-picture** — this folder is **support documentation**; also read root `docs/*.md`, `src/` code, and kit docs before decisions.
