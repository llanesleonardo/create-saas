# Software Patterns Docs

Shared design & architecture patterns for better coding decisions.

**Do not hand-author the full tree in each product.** Sync from the package:

```bash
npm install @llanesleonardo/software-patterns-docs --save-dev
npx software-patterns-docs-sync
# or: npx @llanesleonardo/create-saas sync-docs --dir .
```

That fills this folder from `@llanesleonardo/software-patterns-docs`.

**Agents:** use the **saas-docs-picture** skill — category indexes here are **support docs**; also read repo code and kit docs before implementing.
